import { STATUS_META } from '../utils/constants';
export default function TableGrid({ tables = [], onSelect }) { return <div className="table-grid">{tables.map((table) => { const status = String(table.status || 'available').toLowerCase(); const meta = STATUS_META[status] || { label: status, tone: 'neutral' }; return <button className={`table-card table-card--${meta.tone}`} key={table.id || table._id || table.number} type="button" onClick={() => onSelect?.(table)}><div className="table-card__top"><span className={`table-status-dot table-status-dot--${meta.tone}`}/><span>{meta.label}</span></div><div className="table-card__number">T{table.number ?? table.tableNumber}</div><div className="table-card__meta">{table.capacity || '—'} seats {table.section ? `• ${table.section}` : ''}</div></button>; })}</div>; }
